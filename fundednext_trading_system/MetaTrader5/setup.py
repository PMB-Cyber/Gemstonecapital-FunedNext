from setuptools import setup, find_packages

setup(
    name="MetaTrader5",
    version="5.0.45",
    packages=find_packages(),
    description="A mock MetaTrader5 package for development on non-Windows systems.",
)
